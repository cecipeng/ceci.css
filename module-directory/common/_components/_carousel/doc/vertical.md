## 基本垂直轮播

`ui-carousel`上添加类名`ui-carousel--vertical`。

创建实例的参数需加入`horizontal: true`：
```
var slider2 = new Slider({
    ele : "#carousel2",
    isCSS3: true,
    isturn: true,
    nsnav: true,
    autoPlay: false,
    horizontal: true
});
```

